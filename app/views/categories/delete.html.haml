= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link')
%div
  %h2 Delete Category
  = form_for(:category, :url => {:action => 'destroy', :id => @category.id}) do |f|
    %p Are you sure you want to permanently delete this category?
    %p= category.name
    .form-buttons
      = submit_tag("Delete category")
